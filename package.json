{
    "name": "airline",
    "version": "0.0.1",
    "devDependencies": {
        "ts-patch": "^2.1.0",
        "typescript": "^4.5.2"
    },
    "scripts": {
        "build": "ts-patch install -s"
    }
}